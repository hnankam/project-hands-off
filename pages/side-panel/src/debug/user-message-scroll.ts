/**
 * User-message edit scroll diagnostics. Console filter: `user-msg-scroll`.
 * Set to `false` when done. Optional: gate with
 * `import.meta.env.VITE_DEBUG_USER_MESSAGE_SCROLL === 'true'` for env-only control.
 */
export const DEBUG_USER_MESSAGE_SCROLL = false;
