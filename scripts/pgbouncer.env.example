# PgBouncer Configuration
USE_PGBOUNCER=true
PGBOUNCER_HOST=localhost
PGBOUNCER_PORT=6432

# Original PostgreSQL Connection (keep these for direct access if needed)
DB_HOST=ep-billowing-surf-aatf87vk-pooler.westus3.azure.neon.tech
DB_PORT=5432
DB_DATABASE=handsoff_db
DB_USERNAME=neondb_owner
DB_PASSWORD=npg_IJl5XdnPE6FW
DB_OTHER_PARAMS=sslmode=require

# Application pool settings (can be more aggressive with PgBouncer)
DB_POOL_MIN_SIZE=2
DB_POOL_MAX_SIZE=20
